{
  "name": "dota-app",
  "version": "1.0.0",
  "description": "An app to help you make better hero and item selections in DOTA 2",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "export NODE_ENV=test && jasmine",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^6.2.0",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "jasmin": "0.0.2",
    "jasmine": "^3.3.1",
    "request": "^2.88.0"
  }
}
